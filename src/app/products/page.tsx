import { KeyFeatures, Product, Series, SwappableModules } from "@/sections";

const Products = () => {
  return (
    <main>
      <Series />
      <KeyFeatures />
      <Product />
      <SwappableModules />
    </main>
  );
};

export default Products;
